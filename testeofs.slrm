#!/bin/bash -l

#SBATCH -N 37
#SBATCH -p debug
#SBATCH -t 00:30:00
#SBATCH --ccm
#SBATCH --qos=premium

bash
module load spark/1.5.1 sbt
export SPARK_LOCAL_DIRS="/tmp,/dev/shm"
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:`pwd`/lib
start-all.sh

# generate the eofs
./src/testEOFs.sh /global/cscratch1/sd/gittens/large-scale-climate/target/scala-2.10/large-scale-climate-assembly-0.0.1.jar

exit
